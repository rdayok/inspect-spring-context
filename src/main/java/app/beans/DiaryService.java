package app.beans;

import org.springframework.stereotype.Service;

// @Component annotation is the generic stereotype annotation for application Bean
// So using the at component would work fine but for convention we use the more specific annotation
@Service
public class DiaryService {
}
